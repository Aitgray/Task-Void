export const AGING_RATE = 1;    // priority points added per hour a task waits
export const SKIP_WEIGHT = 0.5; // priority points added per skip
export const JITTER_MAX = 0.5;  // upper bound of random noise added to each score
